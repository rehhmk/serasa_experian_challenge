# URL do backend Spring Boot que o dev server do Vite deve usar como proxy
# para /api/* (ver vite.config.ts). Copie para .env.local e ajuste se o
# backend não estiver em localhost:8080 (ex: rodando via docker-compose).
VITE_API_PROXY_TARGET=http://localhost:8080
